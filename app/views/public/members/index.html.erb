<h1>メンバー一覧</h1>

  <% @members.each do |member| %>

  <h3>写真</h3>
  <span><%= member.image %></span>

  <h3>名前</h3>
  <span><%= member.name %></span>

  <%= link_to"詳細ページへ", member_path(@member.id) %>

<% end %>